# Magento 2 – Custom CLI Command Module

This module demonstrates how to create custom CLI (Command Line Interface) commands in Magento 2 using the Symfony Console component.

The module includes two CLI commands:
1. A simple command without arguments
2. A command that accepts arguments with a default value

---

## 📌 Module Purpose

The purpose of this module is to showcase:
- How Magento 2 CLI commands work internally
- How to register custom commands using Dependency Injection
- How to handle command arguments cleanly

This module is suitable for learning, automation tasks, and interview demonstrations.



